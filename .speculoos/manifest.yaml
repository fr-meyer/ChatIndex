schema_version: 0.1
project: ChatIndex
workflow: speculoos-v0
canonical_task: pr-000-fork-dev-baseline
repository:
  upstream: VectifyAI/ChatIndex
  fork: fr-meyer/ChatIndex
  local_path: /home/node/.openclaw/repos/ChatIndex
  base_branch: dev
  working_branch: chore/fork-dev-baseline
  upstream_commit: 7df2c9208db6f113f85a6c09295bec7f0f2114e7
status:
  phase: repo-local-contract
  pret_a_coder: blocked
  blocker_summary:
    - fr-meyer/ChatIndex is not visible from current GitHub access.
    - gh is installed in the OpenClaw container but unauthenticated.
    - Docker is unavailable inside the OpenClaw container for sidecar control.
    - tmux is not installed in the OpenClaw container.
validation:
  no_secret_tests:
    status: passed
    command: . .venv/bin/activate && python -m unittest discover -v
    result: 2 passed, 0 failed
surfaces:
  manifest:
    enabled: true
    authority: canonical-v0
  github_issue:
    enabled: true
    status: blocked
    blocked_by: fork-or-auth-unavailable
    file: .speculoos/surfaces/github.yaml
  github_project:
    enabled: true
    status: blocked
    blocked_by: fork-or-auth-unavailable
    file: .speculoos/surfaces/github.yaml
  vibe_kanban:
    enabled: true
    status: pending
    blocked_by: sidecar-not-started
    file: .speculoos/surfaces/vibe-kanban.yaml
lanes:
  codex_review:
    enabled: true
    role: baseline-review
    contract: .speculoos/lane-contracts/codex-review.md
  cursor_worker:
    enabled: true
    role: bounded-implementation
    contract: .speculoos/lane-contracts/cursor-worker.md
  opencode:
    enabled: false
    role: later-open-executor-candidate
  omp:
    enabled: false
    role: later-advanced-executor-candidate
  hermes:
    enabled: false
    role: later-advanced-executor-harness-candidate
gates:
  before_executor:
    - scope-bounded
    - acceptance-criteria-present
    - write-paths-explicit
    - branch-worktree-ready
    - credential-policy-explicit
    - tests-known
  before_publish:
    - human-approval
    - git-auth-available
    - fork-visible
    - tests-pass
    - private-data-check-pass
